async function sendPost(event) {
  event.preventDefault();

  const // grab team-type
  const //grab team-name
  
  //going to send fetch request to team-routes
  //method: post

}

//button addEventListener sendPost function